apiVersion: v1
kind: Service
metadata:
  name: equipment-tracker-service
spec:
  selector:
    app: equipment-tracker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort